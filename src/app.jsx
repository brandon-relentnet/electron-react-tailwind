import * as React from 'react';
import { createRoot } from 'react-dom/client';

const root = createRoot(document.body);
root.render(<h1 className="font-bold text-2xl underline text-red-700">Hello react</h1>);